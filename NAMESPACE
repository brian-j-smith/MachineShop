import(methods)

importClassesFrom(kernlab, ksvm)
importClassesFrom(party, RandomForest)

importFrom(caret, createMultiFolds, createResample)
importFrom(foreach, foreach, "%dopar%")
importFrom(Hmisc, rcorr.cens)
importFrom(irr, kappa2)
importFrom(magrittr, "%>%")
importFrom(MLmetrics, MultiLogLoss, PRAUC)
importFrom(ModelMetrics, auc, brier, ce, kappa, mae, rmse, sensitivity,
           specificity)
importFrom(stats, approx, cor, median, model.frame, model.matrix,
           model.response, predict, sd, terms, update)
importFrom(survival, is.Surv, Surv, survfit)
importFrom(survivalROC, survivalROC)
importFrom(utils, tail)


export(BootControl,
       CVControl)

exportClasses(MLControl)

export(MLModel,
       CForestModel,
       CoxModel,
       CoxStepAICModel,
       GBMModel,
       GLMModel,
       GLMStepAICModel,
       GLMNetModel,
       KSVMModel,
       NNetModel,
       RandomForestModel,
       SurvRegModel,
       SurvRegStepAICModel)

exportClasses(MLModel,
              MLModelFit)

exportMethods(fit,
              resample)

S3method(predict, MLModelFit)
S3method(summary, Resamples)
