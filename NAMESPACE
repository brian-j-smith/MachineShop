import(methods)
import(survival)

importClassesFrom(party, RandomForest)

importFrom(caret, createMultiFolds)
importFrom(foreach, foreach, "%dopar%")
importFrom(Hmisc, rcorr.cens)
importFrom(irr, kappa2)
importFrom(magrittr, "%>%")
importFrom(ModelMetrics, auc, brier, ce, kappa, mae, rmse, sensitivity,
           specificity)
importFrom(stats, approx, cor, median, model.frame, model.matrix,
           model.response, predict, sd, terms, update)
importFrom(survivalROC, survivalROC)
importFrom(utils, tail)


export(fit,
       resample)

export(CVControl)

exportClasses(AbstractControl,
              CVControl)

export(CForestModel,
       CoxModel,
       CoxStepAICModel,
       GBMModel,
       GLMNetModel)

exportClasses(AbstractModel,
              CForestModel,
              CoxModel,
              CoxStepAICModel,
              GBMModel,
              GLMNetModel)

S3method(predict, CForestFit)
S3method(predict, CoxFit)
S3method(predict, GBMFit)
S3method(predict, GLMNetFit)
S3method(summary, Resamples)
